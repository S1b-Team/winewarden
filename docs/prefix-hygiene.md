# Prefix Hygiene

Prefixes are living ecosystems. Stability matters more than tweaks.

Hygiene features:
- Detect orphaned DLLs
- Detect conflicting overrides
- Separate prefixes by trust tier
- Lightweight snapshots before new executables
